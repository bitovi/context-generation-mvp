# context-generation-mvp



Use: `npm run start` to execute from settings in `issue-to-context.sample.yaml`

- Temporary repo is checked out into `/repo-tmp`
- Instruction temporary files will be constructed in `/tmp`